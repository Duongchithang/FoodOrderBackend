'use strict';

/**
 * category-order-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::category-order-card.category-order-card');
